[project]
name = "first-miniproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pannakotta", email = "panissimo13@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
first-miniproject = "first_miniproject:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
